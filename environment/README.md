This directory contains the environment manifests that were used during development.

- `RLSemanticCaching.yml`: full conda export from the working environment.
- `RLSemanticCaching.from-history.yml`: minimal history-based conda export.

For a fresh public setup, the README in the release root is the primary source of truth. The full export is included mainly for reproducibility on similar systems.
